<% if line_item == @current_item %>
<tr id="current_item">
<% else %>
<tr>
<% end %>

  <li class="list-group-item">
  <%= line_item.product.title %>
  <%= number_to_currency(line_item.total_price) %>
  <span class="badge"><%= line_item.quantity %></span></li>

<!--<%= sprintf("%2d x %s",
            line_item.quantity,
            truncate(line_item.product.title, length: 50)) %>-->